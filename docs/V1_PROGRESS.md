# تَأْثِيل — V1 Progress

## 24 سبتمبر 2026

### مكتمل
- تثبيت `taatheel.result.v1` وRouter V1 من مرحلة التجميد السابقة.
- إنشاء bucket خاص `taatheel-v1-uploads` للملفات، غير عام، بحد 8 MB وأنواع MIME محددة.
- إضافة `expires_at` إلى `content_assets` وربط الرفع المجهول بسياسة احتفاظ 24 ساعة.
- نشر Edge Function باسم `taatheel-ingest` تستقبل الملف، تتحقق من النوع والحجم، تحسب SHA-256، تحفظه في Storage، وتنشئ سجل `content_assets`.
- استخراج نص فعلي لملفات TXT/Markdown وحفظه في `content_extractions`.
- نشر `taatheel-cleanup` لحذف ملفات Storage المنتهية وأرشفة سجل الأصل مع إبقاء بيانات الاستخراج/التدقيق.
- جدولة تنظيف الملفات المنتهية كل ساعة.

### اختبار فعلي
تم رفع ملف UTF-8 عربي حقيقي باسم `v1-ingest-test.txt` عبر `taatheel-ingest`، وحُفظ في Storage، وسُجل SHA-256 والحجم ووقت الانتهاء، واستُخرج النص العربي بنجاح إلى `content_extractions`. أعادت الوظيفة `taatheel.ingest.v1` وحالة `ready`.

### المتبقي في مسار رفع الملفات
1. ربط واجهة الويب بـ `taatheel-ingest` بدل الاكتفاء باسم الملف.
2. استخراج PDF وDOCX فعليًا.
3. استخراج metadata للصور والصوت والفيديو.
4. تمرير النص/metadata المستخرج إلى Router V1.
5. اختبار دورة حذف ملف منتهٍ بالكامل والتحقق من Storage والسجل.

### ملاحظة نطاق
لا توجد إضافة وظيفية جديدة هنا؛ هذه الأعمال تحقق معيار V1 المجمد: رفع ملفات فعلي + استخراج المحتوى/metadata + حذف الملفات بعد 24 ساعة.

### تحديث 24 سبتمبر 2026 — ربط الواجهة بالرفع الفعلي
- رُبطت واجهة `prototype/app-v4.js` بـ `taatheel-ingest` بدل الاكتفاء باسم الملف.
- عند اختيار ملف، تحوّله الواجهة إلى Base64 وترسله مع MIME ونوع المحتوى إلى مسار ingest.
- ملفات TXT/Markdown تستخدم النص المستخرج فعليًا كمدخل إلى Router V1؛ أما الأنواع التي لم يكتمل استخراجها فتستمر مؤقتًا بالوصف/اسم الملف مع إظهار أن الاستخراج المتخصص قيد الاستكمال.
- تم تحديث Build إلى `V1-INGEST-UI-1`.
- تحقق ساكن بعد الالتزام أكد وجود استدعاء ingest، تحويل الملف، التسلسل ingest → router، وتحديث وسم البناء.
- Commit الواجهة: `743bb41c71ad219891cdcaecbd9b784156179b6c`.

### المتبقي التالي
1. استخراج PDF وDOCX فعليًا.
2. استخراج metadata للصور والصوت والفيديو.
3. اختبار تكاملي من الواجهة المنشورة إلى Storage/Extraction/Router بعد انتشار GitHub Pages.
4. اختبار دورة الحذف المنتهي بالكامل.
